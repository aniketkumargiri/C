#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a;
    float b;
    printf("enter value of a,b");
    scanf("%d%f", &a, &b);
    printf("%d %f ", a, b);

    return 0;
}
/* variable-its value often changes during the execution of the program
constants-its value remain same throughout the program
1.numeric constant:
a.integer(decimal- 0 to 9,octal- 0 to 7,hexa-decimal - 0 to 9 & a to f)
b.real
2.character constant:
a.single character constant('a','1')
b.string("ani","giri")
*/