#include <stdio.h>
int main()
{
    printf("hello ani to the world of cp\n");
    printf("hello\n");
    printf("gcc file name.c+ enter then file name+tab+enter to run on terminal\n");
    printf("in folder, click on filename.exe then click on run executable and finally on the right hand side of the code runner, click on run executable ");
    for (int i = 0; i < 5; i++)
    {
        for (int j = 0; j < i; j++)
        {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
