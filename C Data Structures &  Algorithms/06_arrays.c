#include <stdio.h>

int main(int argc, char const *argv[])
{
    printf("Arrays and Abstract Data Types\n");

    return 0;
}

// ADT(Abstract Data Types): Minimal required functionality for performing some operations

// Abstraction: Providing only the required functionality and uses without revealing the implementation details

// Arrays: Collection of elements of the same data type in contiguous memory locations which is easily accessible by an index in O(1);

// Minimal functionality
// get(i) : get element i;
// set(i,num): set element i to num;

// opertions :
// max()
// min()
// search(num)
// insert(i,num)
// append(x)

// static array: size cannot be changed
// dynamic array: size can be changed
