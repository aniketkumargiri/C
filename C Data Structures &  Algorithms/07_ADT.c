#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    printf("Array as an Abstract Data Type\n");

    return 0;
}

// ADTs : set of values + set of operations
// myArray --> total_size               max()
// used_size      +         get(i)
// base address             set(i,num)

// ptr = (int *)malloc(tSize * sizeof(int));
