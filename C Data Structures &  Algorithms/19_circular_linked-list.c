#include<stdio.h>

int main(int argc, char const *argv[])
{
    printf("Circular linked list and Operations in data structures\n");

    return 0;
}

// Circular linked list:

// Similar to singly linked list
// Pointer to the last node points to the head of the linked list.
// Rest all the implementations are same as the singly linked list