#include <stdio.h>

// Typecasting Syntax
// (type) value;

int main(int argc, char const *argv[])
{
    // int a;
    // float b;
    // char c;
    // printf("Enter the value of a\n");
    // scanf("%d", &a);
    // printf("A is %d\n", a);
    // printf("Enter the value of b\n");
    // scanf("%f", &b);
    // printf("B is %d\n", (int)b);
    // printf("Type any character : \n");
    // scanf(" %c", &c);
    // printf("Character is %d", (int)c);

    int p = 3;
    float q = (float)54 / 5;
    printf("The Value of q is %.2f\n", q);

    return 0;
}