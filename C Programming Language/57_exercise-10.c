#include<stdio.h>

int main(int argc, char const *argv[])
{
    printf("Write a c program to multiply two given matrices.\n");

    return 0;
}

// Conditions for Matrix multiplication:

// number of columns in first matrix should be equal to the number of rows in second matrix
// Resultant matrix must have the same number of rows as in first matrix and the same number of columns as in second